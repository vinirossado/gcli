package repository_test

import (
	"context"
	"log"
	"os"
	"testing"
	"time"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/testcontainers/testcontainers-go"
	tcpostgres "github.com/testcontainers/testcontainers-go/modules/postgres"
	"github.com/testcontainers/testcontainers-go/wait"
	"go.uber.org/zap"
	gpostgres "gorm.io/driver/postgres"
	"gorm.io/gorm"

	"{{ .ProjectName }}/pkg/logger"
	"{{ .ProjectName }}/source/model"
	"{{ .ProjectName }}/source/repository"
)

var (
	testDB  *gorm.DB
	testLog *logger.Logger
)

// TestMain spins up a real Postgres container once for all tests in this file,
// runs the suite, then tears it down. Each test is responsible for cleaning
// up the rows it creates via t.Cleanup so tests do not interfere.
func TestMain(m *testing.M) {
	ctx := context.Background()

	pgContainer, err := tcpostgres.Run(ctx, "postgres:16-alpine",
		tcpostgres.WithDatabase("testdb"),
		tcpostgres.WithUsername("test"),
		tcpostgres.WithPassword("test"),
		testcontainers.WithWaitStrategy(
			wait.ForLog("database system is ready to accept connections").
				WithOccurrence(2).
				WithStartupTimeout(30*time.Second),
		),
	)
	if err != nil {
		log.Fatalf("failed to start postgres container: %v", err)
	}
	defer pgContainer.Terminate(ctx)

	connStr, err := pgContainer.ConnectionString(ctx, "sslmode=disable")
	if err != nil {
		log.Fatalf("failed to get connection string: %v", err)
	}

	testDB, err = gorm.Open(gpostgres.Open(connStr), &gorm.Config{})
	if err != nil {
		log.Fatalf("failed to connect to postgres: %v", err)
	}
	if err = testDB.AutoMigrate(&model.{{ .StructName }}{}); err != nil {
		log.Fatalf("failed to auto-migrate: %v", err)
	}

	testLog = &logger.Logger{Logger: zap.NewNop()}
	os.Exit(m.Run())
}

func new{{ .StructName }}Repo() repository.{{ .StructName }}Repository {
	return repository.New{{ .StructName }}Repository(repository.NewRepository(testLog, testDB))
}

func new{{ .StructName }}Entity() *model.{{ .StructName }} {
	return &model.{{ .StructName }}{
{{ if .Properties }}{{ range $name, $type := .Properties }}		{{ $name }}: {{ testValue $type }},
{{ end }}{{ end }}	}
}

func Test{{ .StructName }}_Create(t *testing.T) {
	repo := new{{ .StructName }}Repo()
	ctx := context.Background()

	id, err := repo.Create{{ .StructName }}(ctx, new{{ .StructName }}Entity())
	require.NoError(t, err)
	assert.Greater(t, id, uint(0))

	t.Cleanup(func() { testDB.Unscoped().Delete(&model.{{ .StructName }}{}, id) })
}

func Test{{ .StructName }}_GetById(t *testing.T) {
	repo := new{{ .StructName }}Repo()
	ctx := context.Background()

	id, err := repo.Create{{ .StructName }}(ctx, new{{ .StructName }}Entity())
	require.NoError(t, err)
	t.Cleanup(func() { testDB.Unscoped().Delete(&model.{{ .StructName }}{}, id) })

	found, err := repo.Get{{ .StructName }}ById(ctx, id)
	require.NoError(t, err)
	assert.Equal(t, id, found.ID)
}

func Test{{ .StructName }}_GetAll(t *testing.T) {
	// Truncate first so the count assertion is not affected by other tests.
	testDB.Unscoped().Where("1 = 1").Delete(&model.{{ .StructName }}{})
	t.Cleanup(func() { testDB.Unscoped().Where("1 = 1").Delete(&model.{{ .StructName }}{}) })

	repo := new{{ .StructName }}Repo()
	ctx := context.Background()

	for range 3 {
		_, err := repo.Create{{ .StructName }}(ctx, new{{ .StructName }}Entity())
		require.NoError(t, err)
	}

	all, err := repo.GetAll{{ .StructName }}(ctx)
	require.NoError(t, err)
	assert.Len(t, *all, 3)
}

func Test{{ .StructName }}_Update(t *testing.T) {
	repo := new{{ .StructName }}Repo()
	ctx := context.Background()

	entity := new{{ .StructName }}Entity()
	id, err := repo.Create{{ .StructName }}(ctx, entity)
	require.NoError(t, err)
	t.Cleanup(func() { testDB.Unscoped().Delete(&model.{{ .StructName }}{}, id) })

	entity.ID = id
	_, err = repo.Update{{ .StructName }}(ctx, entity)
	require.NoError(t, err)
}

func Test{{ .StructName }}_Delete(t *testing.T) {
	repo := new{{ .StructName }}Repo()
	ctx := context.Background()

	id, err := repo.Create{{ .StructName }}(ctx, new{{ .StructName }}Entity())
	require.NoError(t, err)
	// Hard-delete after the test so the row does not linger (soft-delete leaves it in the table).
	t.Cleanup(func() { testDB.Unscoped().Delete(&model.{{ .StructName }}{}, id) })

	err = repo.Delete{{ .StructName }}(ctx, id)
	require.NoError(t, err)

	_, err = repo.Get{{ .StructName }}ById(ctx, id)
	assert.Error(t, err, "record should be unreachable after soft-delete")
}
